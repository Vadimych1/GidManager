import 'package:ar_flutter_plugin/ar_flutter_plugin.dart';

class ARMod {
  static ARView arv =
      ARView(onARViewCreated: (session, object, anchor, location) {
    // TODO: AR Module
    // ? https://blog.logrocket.com/build-augmented-reality-app-flutter/
  });
}
